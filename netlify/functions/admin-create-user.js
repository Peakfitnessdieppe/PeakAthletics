exports.handler = async function () {
  return {
    statusCode: 200,
    body: JSON.stringify({ status: 'stub', message: 'TODO: use service role key to create Supabase auth user, insert profile row' }),
  }
}
